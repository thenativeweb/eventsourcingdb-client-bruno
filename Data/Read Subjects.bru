meta {
  name: Read Subjects
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/read-subjects
  body: json
  auth: bearer
}

auth:bearer {
  token: {{apiToken}}
}

body:json {
  {
    "baseSubject": "/"
  }
}
